{
	"type": "object",
	"properties": {
		"first": {
			"type": "string",
			"format": "only-letters"
		},
		"family": {
			"type": "string",
			"format": "only-letters"
		},
		"email": { "type": "string", "format": "email" },
		"password": { "type": "string", "format": "strong-password" },
		"confirmPassword": {
			"type": "string",
			"format": "strong-password"
		}
	},
	"required": ["first","family", "email", "password", "confirmPassword"]
}
